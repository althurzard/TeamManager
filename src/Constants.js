export const DEFAULT_COLOR = '#20D99B'
export const ADDRESS = 'localhost' // for Android: 10.0.2.2, for iOS: localhost
export const PORT = 3000
export const MONGODB_URI = `mongodb://${ADDRESS}/TeamManager`
export const DOMAIN_NAME = `http://${ADDRESS}:${PORT}`
export const ERROR_POST = 500

